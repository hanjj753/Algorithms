{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "C++ build (MSYS2 UCRT64)",
      "type": "shell",
      "command": "D:/msys64/ucrt64/bin/g++.exe",
      "args": [
        "-std=c++17",
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/a.exe"
      ],
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"],
      "options": {
        "env": {
          "PATH": "D:\\msys64\\ucrt64\\bin;${env:PATH}"
        }
      }
    }
  ]
}
